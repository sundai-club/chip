export const URLs = Object.freeze({
    LOGIN_URL: '/account/login',
    LOGIN_REDIRECT_URL: '/',
    LOGOUT_REDIRECT_URL: '/account/login',
    ELECTRON_AUTH_REDIRECT_URL: 'charm://oauth-callback/',
});

export const normalLoginEnabled = false;

export const subscriptionBased = true;
